# pispot-authd configuration
# Restart pispot-authd after changing group names:
#   sudo systemctl restart pispot-authd

[groups]
# Unix group whose members get read-only dashboard access.
readonly = pispot-ro

# Unix group whose members get full admin dashboard access.
# Admin always wins if a user is in both groups.
admin    = pispot-admin
